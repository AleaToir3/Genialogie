 {% extends 'base.html.twig' %}

{% block title %}HistoryEven{% endblock %}

{% block body %}
    <h1>personal_even</h1>

    <table class="table">
        <tbody>
          
            <tr>
                <th>Title</th>
                <td>{{ personal_even.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ personal_even.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                {# <td>{{ }}</td>
            </tr> #}
            <tr>
                <th>Video</th>
                {# <td>{{  }}</td> #}
            </tr>
            <tr>
                <th>Music</th>
                {# <td>{{ }}</td> #}
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ personal_even.date ? personal_even.date|date('Y-m-d H:i:s') : '' }}</td> 
            </tr>
        </tbody>
    </table>
 
    <a href="{{ path('Home') }}">back to list</a>

    <a href="{{ path('app_personal_even_edit', {'id': personal_even.id}) }}">edit</a>

    {{ include('personal_even/_delete_form.html.twig') }} 
{% endblock %}
