<form method="post" action="{{ path('app_personal_even_delete', {'id': personal_even.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personal_even.id) }}">
    <button class="btn">Delete</button>
</form>
